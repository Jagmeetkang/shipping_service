spring:
  application:
    name: shipping_service
  datasource:
    url: jdbc:postgresql://localhost:5432/shipping_service_db
    username: shipping_user
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update  # or 'create' if you want to recreate tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 465
    username: noreplyecommercetest@gmail.com
    password: mzlf fgcv tnyr fawz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          tls:
            enable: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: "shipping-group"
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: latest
      properties:
        spring:
          json:
            trusted:
              packages: "*"

server:
  port: 9095
